import 'package:flutter/material.dart';
class ReviewComposerSheet extends StatelessWidget {
  const ReviewComposerSheet({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ReviewComposerSheet')),
      body: const Center(child: Text('Coming soon', style: TextStyle(color: Colors.grey))),
    );
  }
}
