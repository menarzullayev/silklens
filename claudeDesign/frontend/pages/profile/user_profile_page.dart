import 'package:flutter/material.dart';

class UserProfilePagePage extends StatelessWidget {
  const UserProfilePagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('UserProfilePage')),
      body: const Center(
        child: Text('Coming soon...', style: TextStyle(color: Colors.grey)),
      ),
    );
  }
}
