import 'package:flutter/material.dart';

class CheckoutPagePage extends StatelessWidget {
  const CheckoutPagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CheckoutPage')),
      body: const Center(
        child: Text('Coming soon...', style: TextStyle(color: Colors.grey)),
      ),
    );
  }
}
