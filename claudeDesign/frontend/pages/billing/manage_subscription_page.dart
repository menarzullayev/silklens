import 'package:flutter/material.dart';

class ManageSubscriptionPagePage extends StatelessWidget {
  const ManageSubscriptionPagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ManageSubscriptionPage')),
      body: const Center(
        child: Text('Coming soon...', style: TextStyle(color: Colors.grey)),
      ),
    );
  }
}
