import 'package:flutter/material.dart';

class InvoicesPagePage extends StatelessWidget {
  const InvoicesPagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('InvoicesPage')),
      body: const Center(
        child: Text('Coming soon...', style: TextStyle(color: Colors.grey)),
      ),
    );
  }
}
