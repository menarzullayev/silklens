import 'package:flutter/material.dart';

class PlansPagePage extends StatelessWidget {
  const PlansPagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PlansPage')),
      body: const Center(
        child: Text('Coming soon...', style: TextStyle(color: Colors.grey)),
      ),
    );
  }
}
