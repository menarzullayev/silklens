// Single row on a leaderboard. Backend returns them ordered by rank ASC.

enum LeaderboardScope { weekly, monthly, allTime, friends }

class LeaderboardEntry {
  // STUB: LeaderboardEntry factory (codegen pending)
}
